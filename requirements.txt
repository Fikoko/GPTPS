# HTML parsing
beautifulsoup4==4.9.3       # Parses HTML and XML; allows easy DOM traversal
lxml==5.4.0                 # High-performance XML and HTML parser
lxml_html_clean==0.4.2      # Cleans up HTML documents (removes unwanted tags)
soupsieve==2.7              # Used by BeautifulSoup to select elements with CSS selectors
html5lib==1.1               # HTML parser (used for backwards compatibility)

# HTTP requests & browser automation
requests==2.32.4            # HTTP library for fetching web pages
urllib3==2.5.0              # Underlying HTTP client used by requests
certifi==2025.8.3           # SSL certificates (often used with requests)
playwright==1.52.0          # Automates browsers (good for JS-heavy pages)

# System / OS
psutil==7.0.0               # Process and system info (CPU, memory usage, running processes)
tzlocal==5.3.1              # Gets local timezone

# Build & C extensions
setuptools>=80.9.0         # Build and package Python modules
wheel==0.45.1               # Packaging Python libraries as wheels
Cython==3.1.3               # Converts Python code to C for speed
pybind11==3.0.0             # Bridges C++ code with Python (used in C++ speed modules)

# Numerical / scientific
numpy>=1.26.0               # Core numerical computing library (arrays, linear algebra)
scipy==1.16.1               # Scientific computing functions (optimization, integration, stats)
scikit-learn==1.2.2         # Classical ML algorithms (classification, regression, clustering)
joblib==1.2.0               # Efficient serialization of Python objects, used by scikit-learn
threadpoolctl==3.6.0        # Controls thread pools used in ML libraries
mpmath==1.3.0               # Arbitrary-precision mathematics
networkx==3.5               # Graph and network analysis
triton==3.4.0               # GPU kernel compilation / optimization (often for PyTorch)

# ML / NLP
python-crfsuite==0.9.11     # Conditional Random Fields for sequence labeling
sklearn-crfsuite==0.3.6     # Wrapper for python-crfsuite compatible with scikit-learn
onnxruntime==1.22.1         # Runtime for ONNX models (AI/ML model execution)
protobuf==6.32.0            # Google Protocol Buffers serialization

# Text / datetime utilities
babel==2.17.0               # Internationalization / date formatting
coloredlogs==15.0.1         # Color-coded logging
dateparser==1.1.0           # Parses dates from natural language strings
filelock==3.19.1            # Locks files to prevent concurrent writes
flatbuffers==25.2.10        # Efficient data serialization
fsspec==2025.7.0            # Abstract filesystem specification (local + remote)
ftfy==4.4.3                 # Fixes text encoding issues (e.g., bad Unicode)
humanfriendly==10.0          # Converts file sizes, durations, and numbers into human-readable format
idna==3.10                  # Supports internationalized domain names in URLs
iniconfig==2.1.0            # For parsing .ini config files
python-dateutil==2.9.0.post0 # Parsing and manipulating dates
regex==2025.7.34            # Advanced regular expressions
six==1.17.0                 # Python 2 / 3 compatibility
tabulate==0.9.0             # Pretty-print tables
tld==0.12.6                 # Extracts top-level domains from URLs
tqdm==4.67.1                # Progress bars
wcwidth==0.2.13             # Determines character width in terminal output
webencodings==0.5.1         # Needed for HTML parsing
jusText==3.0.2              # Extracts main textual content from HTML pages
htmldate==0.7.2             # Extracts date information from HTML pages
pyee==13.0.0                # Event-emitting pattern for Python
Pygments==2.19.2            # Syntax highlighting

# Templating / serialization
Jinja2==3.1.6               # Templating engine (HTML or text templates)
MarkupSafe==3.0.2           # Ensures safe strings for templating
packaging==25.0             # Version and dependency parsing
pluggy==1.6.0               # Plugin management (used by pytest)
flatbuffers==25.2.10        # Efficient data serialization

# Optional concurrency / git
git-filter-repo==2.47.0     # Git repo management
greenlet==3.2.4             # Concurrency primitive used by some async frameworks
typing_extensions==4.14.1   # Adds typing features for older Python versions
